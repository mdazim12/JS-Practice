
    const numbers = [1, 4, 5, 7, 9];

    for (const num of numbers){
        // console.log(num)
    }

    const nobab = 'Siraj ul dullah';
     for(const char of nobab){
        console.log(char)
     }

     const glass = {
        name : 'glass',
        color : 'goldedn',
        price : 10,
        isClean : true,
    }

    const keys = Object.keys(glass);

    for(const kk of keys){
        console.log(kk)
    }

    FOR IN