
    const numbers = [12 ,10,15 ,7]

    const dobule = numbers.map( n => n * 2)
    // console.log(dobule)
    const fiveBouns = numbers.map( n => n + 5)
    // console.log(fiveBouns)
    
    const half = numbers.map(num => num / 2)
    // console.log(half)

    const friends = ['Tom' ,'Jerry', 'Michel', 'Oliveya'];
    const length = friends.map( friends => friends.length )
    console.log(length)
    const fristLatter = friends.map( friends => friends[0])
    console.log(fristLatter)
