
    const add = (first, secound) =>{
        return first + secound;
    }

    const multiply = (first, secound) =>{
        return first + secound;
    }

    const devideTheFristNumberByTheSecondNumber = (firstr , secound) =>{
        return firstr / secound;
    }

    // export default add;

    export {add , multiply, devideTheFristNumberByTheSecondNumber};